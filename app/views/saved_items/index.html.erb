<h1>Listing saved_items</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @saved_items.each do |saved_item| %>
  <tr>
    <td><%= link_to 'Show', saved_item %></td>
    <td><%= link_to 'Edit', edit_saved_item_path(saved_item) %></td>
    <td><%= link_to 'Destroy', saved_item, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Saved item', new_saved_item_path %>
