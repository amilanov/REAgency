<h1>Gradovi</h1>

<table>
  <tr>
    <th>Naziv grada</th>
    <th>Poštanski broj</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @cities.each do |city| %>
  <tr>
    <td><%= city.cityName %></td>
    <td><%= city.postalCode %></td>
    <td><%= link_to 'Detalji', city %></td>
    <td><%= link_to 'Uredi', edit_city_path(city) %></td>
    <td><%= link_to 'Obriši', city, confirm: 'Da li ste sigurni?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Novi grad', new_city_path %>
